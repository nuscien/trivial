# Trivial.CommandLine

In `Trivial.CommandLine` namespace of `Trivial.dll` [library](../). The useful utilities for console application.

- [Arguments](./arguments) An instance provided a way to access values from the arguments and dispatch the command verbs.
